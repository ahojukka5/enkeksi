name: Release 1.0 once

on:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: master
          fetch-depth: 0
      - name: Remove one-shot workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/release-1.0-once.yml
          git commit -m "Remove one-shot release workflow"
          git push origin HEAD:master
      - name: Create v1.0.0 release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git tag -a v1.0.0 -m "enkeksi 1.0.0"
          git push origin v1.0.0
          gh release create v1.0.0 \
            --title "enkeksi 1.0.0" \
            --notes "First stable release of the modernized enkeksi Markdown-SQL evaluator. See CHANGELOG.md for the complete feature and migration summary."
